<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout

xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"

    android:layout_height="wrap_content"
    android:minHeight="40dp"
    android:gravity="center"
    android:id="@+id/relativeLayout">

    <CheckBox
        android:id="@+id/check_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:gravity="center"
        android:textColor="?attr/textcolor"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/item_data"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:background="@null"
        android:gravity="center|left|start"
        android:minWidth="250dp"
        app:layout_constraintBaseline_toBaselineOf="@+id/check_item"
        app:layout_constraintBottom_toBottomOf="@+id/check_item"
        app:layout_constraintLeft_toRightOf="@+id/check_item"
        app:layout_constraintStart_toEndOf="@+id/check_item"
        android:layout_marginLeft="24dp"
        android:textColor="?attr/textcolor"/>

    <ImageView
        android:id="@+id/cancelitem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_cancel_black_24dp"
        app:layout_constraintBottom_toBottomOf="@+id/check_item"
        app:layout_constraintEnd_toEndOf="parent"
        android:outlineAmbientShadowColor="?attr/textcolor"
        app:layout_constraintRight_toRightOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>

